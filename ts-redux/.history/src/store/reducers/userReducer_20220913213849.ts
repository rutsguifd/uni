
const initialState = {
    users: [],
    loading: false,
    error: null
}

export const userReducer = (state, action) => {
    switch(action.type) {
        
        
        default:
            return state;
    }
}