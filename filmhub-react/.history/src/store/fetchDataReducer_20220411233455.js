
const initialState = [];

export const fetchDataReducer = (state = initialState, action) => {
    switch(action.type) {
        case "":
            return 

        default:
            return state;
    }
}